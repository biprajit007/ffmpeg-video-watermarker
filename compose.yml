services:
  ffmpeg-tool:
    build:
      context: .
    container_name: ffmpeg-video-watermarker
    command: python app.py --help
    volumes:
      - ./data:/data
      - ./output:/output
      - ./config:/config
    environment:
      PYTHONUNBUFFERED: "1"
